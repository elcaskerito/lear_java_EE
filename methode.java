package lear_java_EE;

public class methode {
    /**
     * quelque méthode
     * toUpperCase()
     * toLowerCase()
     * a.concat(b)
     * a.equals(b)
     * A.length()
     * charAt()
     * chaine.substring(indice de debut,indice de fin) : extraire une chaine
     * chaine.indexOf('caractere') : envoie l'ocurrence du caractère chercher dans la chaine
     * chaine.lastIndexOf(caractère) : dernier occurence du caractère chercher
     * var=Math.random()
     * var = Math.pow(valeur)
     * var = Math.abs(valeur)
     */
    
}
